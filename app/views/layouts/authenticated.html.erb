<!DOCTYPE html>
<html>
  <head>
    <title>Calculadora de Prescrição</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap 5 (CSS via CDN) -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    >

    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light">
    <div class="container-fluid">
      <div class="row min-vh-100">
        <!-- Sidebar -->
        <aside class="col-12 col-md-3 col-lg-2 bg-white border-end p-3 d-flex flex-column">
          <div class="mb-4">
            <h1 class="h5 mb-0">Calculadora</h1>
            <small class="text-muted">Área do usuário</small>
          </div>

          <div class="mb-4">
            <div class="fw-semibold">
              <%= (current_user.nome.presence || current_user.email) %>
            </div>
            <div class="small text-muted">Logado</div>
          </div>

          <nav class="nav flex-column mb-auto">
            <span class="nav-link disabled">Menu</span>
            <!-- Links futuros do dashboard entram aqui -->
            <%= link_to "Nova consulta", new_calculation_path, class: "nav-link" %>
            <%= link_to "Histórico", dashboard_path, class: "nav-link" %>
            <% if current_user.admin? %>
              <hr>
              <%= link_to "Admin • Usuários", admin_users_path, class: "nav-link" %>
            <% end %>

          </nav>

          <div class="mt-auto">
            <%= link_to "Logout",
                        destroy_user_session_path,
                        data: { turbo_method: :delete },
                        class: "btn btn-outline-danger w-100" %>
          </div>
        </aside>

        <!-- Conteúdo -->
        <main class="col p-4">
          <% if notice %><div class="alert alert-success"><%= notice %></div><% end %>
          <% if alert %><div class="alert alert-danger"><%= alert %></div><% end %>

          <%= yield %>
        </main>
      </div>
    </div>

    <!-- Bootstrap 5 (JS bundle) -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
